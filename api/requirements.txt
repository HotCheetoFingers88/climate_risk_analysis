# No external dependencies — api/*.py use only the Python standard library
# (http.server, json, urllib, math, csv, os). Listed here so the absence
# of a requirements file isn't mistaken for a missing one.
